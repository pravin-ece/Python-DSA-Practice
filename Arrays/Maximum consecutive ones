class Solution:
    def findMaxConsecutiveOnes(self, nums: List[int]) -> int:
        cnt=0
        cnt1=0
        for i in range(len(nums)):
            if nums[i]==1:
                cnt+=1
            cnt1=max(cnt,cnt1)
            if nums[i]==0:
                cnt=0
        return cnt1
