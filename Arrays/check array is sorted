class Solution:
    def isSorted(self, arr) -> bool:
        a=len(arr)
        for i in range(1,a):
            if arr[i]<arr[i-1]:
                return False
            
        return True
            
