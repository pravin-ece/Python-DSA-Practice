class Solution:
    def rotate(self, arr):
        n=len(arr)
        temp=arr[n-1]
        i=n-1
        while i<n and i!=0:
            arr[i]=arr[i-1]
            i-=1
        arr[0]=temp
        return arr
            
