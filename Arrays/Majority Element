class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        a={}
        n=len(nums)
        for i in nums:
            if i in a:
                a[i]+=1
            else:
                a[i]=1
        for i,j in a.items():
            if j>=n/2:
                return i
