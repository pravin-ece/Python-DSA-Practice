class Solution:
    def reverseArray(self, arr):
        i=0
        n=len(arr)
        j=n-1
        while(i<j):
            arr[i],arr[j]=arr[j],arr[i]
            i+=1
            j-=1
        return arr
